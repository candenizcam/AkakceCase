import SwiftUI

/** This is where I'd write an iOS app if i had a mac
I tried various tricks from my windows but ultimately I wasn't able to test or develop with swift
However, since we use KMM we would be able to use the same viewmodel
(or a slightly adjusted one if needed)
 */
@main
struct iOSApp: App {

    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}